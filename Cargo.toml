[package]
name = "chess"
version = "5.0.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
rand = "0.8"
num_cpus = "1.16.0"
byteorder = "1.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
