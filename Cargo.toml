[package]
name = "chess"
version = "0.2.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
phf = { version = "0.11", features = ["macros"] }
rand = "0.8"
num_cpus = "1.16.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
